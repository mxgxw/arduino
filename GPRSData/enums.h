// Enumerador para deteccion de conexion de modulo GSM.
typedef enum {
  OFFLINE,
  ONLINE
} gsmStatus;

// Enumerador para deteccion de conexion GPRS.
typedef enum {
  DISCONNECTED,
  CONNECTED
} gprsStatus;
